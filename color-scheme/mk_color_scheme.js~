var mkColorScheme = function(initColor) {
    // parameter iniColor ~= '#ABCEDF'
    // this function will Return a HASH which contains:
    // 1. Color Wheel (12 colors in clockwidth
    // 2. use adjacent/traid/split-comp/comp as KEY
    var results = {};

    // convert initColor string into int.
    var initColorHex = parseInt(initColor.split('#')[1],16);
    
    for (var i = 1; i <= 12; i++) {
	
    }

    // decimal number to hex string: using || (NUM).toString(16) ||

    return results;
}
